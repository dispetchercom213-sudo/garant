{
  "name": "garant-beton-monorepo",
  "version": "1.0.0",
  "description": "GARANT BETON - Система управления бетонным заводом",
  "scripts": {
    "install:backend": "cd backend && npm install",
    "install:frontend": "cd frontend && npm install",
    "install:all": "npm run install:backend && npm run install:frontend",
    "build:backend": "cd backend && npx prisma generate && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build": "npm run build:backend",
    "start": "cd backend && npm run start:prod",
    "dev:backend": "cd backend && npm run start:dev",
    "dev:frontend": "cd frontend && npm run dev",
    "prisma:generate": "cd backend && npx prisma generate",
    "prisma:migrate": "cd backend && npx prisma migrate deploy",
    "prisma:seed": "cd backend && npm run prisma:seed"
  },
  "keywords": [
    "concrete",
    "management",
    "nestjs",
    "react",
    "vite"
  ],
  "author": "GARANT BETON Team",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}

